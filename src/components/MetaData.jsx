const MetaData = ({ data }) => {
  return (
    <div className="w-max text-xs bg-white text-gray-800 dark:bg-accentdeep-dark dark:text-gray-50 rounded-xl p-1">
      {data}
    </div>
  );
};

export default MetaData;
